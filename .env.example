# Development defaults for Docker / local runs.
# Copy to `.env` if you use tools that read it (dotenv, Kamal secrets template, etc.).
# The `web` service already sets these in docker-compose.yml; override only when needed.

RAILS_ENV=development
RACK_ENV=development
RAILS_LOG_TO_STDOUT=true
