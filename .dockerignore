.git
.gitignore

# Editor / OS / misc
.DS_Store
.idea
.vscode

# App runtime noise
log
tmp

# Local secrets / env
.env
.env.*
config/master.key

# Bundler caches (we install gems in image)
vendor/bundle

# Frontend / node (not used, but ignore if present)
node_modules
yarn-error.log
npm-debug.log

# Docker/Kamal local state
.kamal

# Local data (not needed in image build context)
storage
db/*.sqlite3
db/*.sqlite3-*
db/*.db

# Project docs / internal planning (keep README in build context)
implementation_status
agent-transcripts
