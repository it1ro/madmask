import { Controller } from "@hotwired/stimulus"

// Placeholder until iteration 5: scene, GLTFLoader, OrbitControls, resize, disconnect.
export default class extends Controller {
  connect() {}

  disconnect() {}
}
