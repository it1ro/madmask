<%= turbo_stream.replace "header_cart_counter", partial: "shared/cart_counter" %>
<%= turbo_stream.replace "header_wishlist_counter", partial: "shared/wishlist_counter" %>
<%= turbo_stream.replace "cart_popover", partial: "cart/popover" %>

<% if params[:product_id].present? %>
  <%= turbo_stream.replace "product_card_actions_#{params[:product_id]}", partial: "products/actions", locals: { product_id: params[:product_id] } %>
<% end %>

<%= turbo_stream.replace "wishlist_page", partial: "wishlist/page" %>
