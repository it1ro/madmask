<%# Landing sections follow in 2b.3+. Layout: shared header/footer + design tokens (2b.2). %>
<div class="flex min-h-[50vh] flex-col items-center justify-center py-16 text-center">
  <p class="text-sm text-[var(--color-text-secondary)]">Madmask — главная (лендинг в разработке)</p>
</div>
