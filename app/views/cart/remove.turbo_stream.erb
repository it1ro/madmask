<%= turbo_stream.replace "header_cart_counter", partial: "shared/cart_counter" %>

<% if @product %>
  <%= turbo_stream.replace "product_card_actions_#{@product.id}", partial: "products/actions", locals: { product: @product } %>
<% end %>

<%= turbo_stream.replace "cart_page", partial: "cart/page" %>
