#!/usr/bin/env ruby
require_relative "../config/boot"
require "bundler/audit/cli"

if ARGV.empty?
  ARGV.concat %w[check --update]
end

if ARGV.include?("check") && !ARGV.include?("--config")
  ARGV.concat %w[--config config/bundler-audit.yml]
end
Bundler::Audit::CLI.start
